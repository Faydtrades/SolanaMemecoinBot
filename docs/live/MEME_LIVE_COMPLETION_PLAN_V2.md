# MEME-LIVE Completion Plan v2 — LOCKED OWNER PLAN

Status: **LOCKED OWNER PLAN / IMPLEMENTATION NOT AUTHORIZED**

This plan is the owner-approved path from the current accepted Phase-4/Phase-5 foundation and completed pre-implementation audit to a technically production-ready, unattended memecoin trading bot capable of live trading under separately approved real-capital policy.

It is governed by `MEME_LIVE_PRODUCTION_CLOSURE_WORKFLOW_V2.md`.

The existing `MEME_LIVE_PRODUCTION_READINESS_PLAN.md` remains an important task-reference, but it must not be executed task-by-task as-is. The audit corrections and lifecycle-first closure architecture take precedence for sequencing and acceptance.

Profitability remains outside the production-engineering finish line.

## Step 1 — Production Closure Architecture / roadmap correction

No production implementation.

Produce one corrected end-to-end architecture that owns all six roadmap gaps and both contract conflicts. Define the complete lifecycle from collector/source health through LIVE admission, immutable approved LIVE amount, chain truth, actual settlement/position projection, funding/risk, venue/quote/plan, final message, sign/send/finality, selected real exit, next trade, crash/reboot and unattended continuation.

Define the permanent Production Lifecycle Matrix and final work-package boundaries.

## Step 2 — Independent architecture review and freeze

CHIEF independently reviews the corrected architecture for missing downstream transitions, incompatible persistence/identity assumptions, recovery dead ends, unsupported PAPER/Shadow promotion and unowned limitations.

Every necessary transition must be VERIFIED, OWNED_NOT_BUILT, HUMAN_EXTERNAL or BLOCKED.

Re-estimate remaining engineering and validation effort after overlap is removed. No implementation starts until the architecture receives project acceptance.

## Step 3 — Activate the dedicated LIVE lane

After separate human authorization, establish the dedicated LIVE branch/control plane (G000) from the then-authoritative clean source baseline. Preserve Phase-4/Phase-5 accepted contracts and Phase-6 research separation.

No signing/send authority is implied by lane activation.

## Step 4 — Source health and public chain truth foundation

Implement G001 and T001 under the corrected architecture.

Deliver explicit collector/source-health deny/recovery semantics, stable producer/source identity prerequisites and dedicated wallet/public-chain read-only truth for SOL, SPL/Token-2022, signatures, transactions and required chain evidence.

Fresh ENTRY denial and protective EXIT obligations must be distinct where required.

Package acceptance requires downstream compatibility with durable LIVE state and later settlement/risk consumers.

## Step 5 — Durable LIVE state, reconciliation and actual settlement

Implement T002/T003 plus the audit-required actual-settlement/position projection.

Create separate durable LIVE logical-intent/attempt/signature state, authoritative finality reconciliation, opening wallet baseline, exactly-once actual token/cash/fee/rent settlement evidence, external/unattributed adjustment handling and reconstructable real-position state.

Shadow expected inventory and PAPER accounting must never be promoted as actual live truth.

## Step 6 — LIVE admission, immutable sizing, risk and funding

Implement the corrected admission/risk boundary around GAP-01/GAP-02/GAP-04 and T004.

Bind canonical candidate provenance to freshness/eligibility, one immutable owner-approved LIVE amount, candidate-level duplicate suppression, spendable native SOL/WSOL/token truth, fee/rent/setup requirements, protected EXIT headroom, reservations, exposure limits and durable kill semantics.

The existing immutable PAPER/Shadow amount is never resized in place.

## Step 7 — Exact final message, signing boundary and send adapter

Implement T005/T006.

Build the final live message from the accepted plan plus explicitly approved ComputeBudget/priority-fee policy, simulate the exact final message, durably bind authority to exact bytes/identity, sign only inside the isolated LIVE boundary, persist before send, submit through the sole bounded mutation adapter and preserve original signed identity through ambiguous outcomes.

No blind new blockhash/signature while the previous transaction may still land.

## Step 8 — Real position, selected exit and continuous producer/runtime composition

Implement T007 plus the audit corrections for actual-position exits, protective-exit obligations and producer lifetime.

Bind each real finalized BUY to actual remaining token units and exactly one owner-approved accepted exit policy/track. Build LIVE SELL intents from actual position state while preserving accepted strategy anchors.

Ensure producer/source continuation supports first, second and later candidates, multiple mints/positions within the declared policy, backlog/freshness handling, source interruption and restart without duplicate logical trades.

## Step 9 — Recovery closure and unattended operations

Implement T008/T009 against the full corrected composition.

Prove every durable cut around admission, sign, send, ACK loss, timeout, RPC failure, finality, settlement, position/exit state and reboot. Recover when authoritative truth resolves uncertainty; retain/quarantine genuinely irreducible ambiguity.

Deliver singleton ownership, heartbeat, alarms, bounded restart/backoff, graceful operator stop, Windows autostart/watchdog and safe reconstruction independent of the dashboard.

## Step 10 — Full deterministic integrated acceptance

Before any external production gate, freeze the source and run a complete fake/local deterministic production-stack acceptance without mainnet mutation.

At minimum prove:

`cold deny -> source/truth -> eligible LIVE admission -> approved size -> funding/risk -> venue/quote/plan -> final message -> simulated send/finality -> actual-settlement fixture -> real-position projection -> selected exit -> SELL -> reconciliation -> next trade -> interruption/restart -> safe continuation`

Include representative UNKNOWN/failure/partial branches, multiple mints/positions within supported policy, protective exits and dry-mode disposition.

No test helper may bypass a production boundary being accepted.

## Step 11 — Final engineering freeze and regression/review

Run the appropriate focused, integrated and broad/full regression on the exact frozen candidate source. CHIEF performs final pre-external-gate system-transition review against the entire lifecycle matrix.

T010 is not started while any required transition remains BLOCKED or OWNED_NOT_BUILT.

## Step 12 — Real-mainnet no-broadcast T010 soak

With separate authorization, run the complete production composition for multiple hours against the real collector, real mainnet RPC and real wallet public state while real signing/send are physically unavailable.

Exercise final message build/simulation, restarts and controlled network/RPC interruption. Non-submitted attempts must have explicit durable disposition and must never create fabricated fills or state drift.

Review and accept the soak evidence before any real-money permission.

## Step 13 — Human T011 preparation and explicit mutation gate

Prepare exact wallet/product/support-state checks, micro-cap policy, selected exit controller, kill/abort conditions and human execution procedure.

Private material remains local and never enters repository/prompts/logs/evidence.

T011 real mutation requires a separate explicit human authorization after all prior gates pass.

## Step 14 — Controlled mainnet micro-capital BUY/SELL smoke

Execute one tightly bounded real BUY and selected-policy SELL/exit through the same production stack intended for ongoing operation.

Verify signature/transaction/finality truth, actual token/cash settlement, fees/rent, real-position projection, exit amount, reconciliation, reservation retirement, restart safety and kill behavior.

Return to a reviewed safe state.

## Step 15 — Final production-readiness review

CHIEF replays the complete Production Lifecycle Matrix and all package evidence, deterministic integrated acceptance, T010 soak and T011 real evidence.

The final closure question is whether any required static or now-live-observed transition for the declared supported unattended production path remains unowned or unresolved.

Any genuine live-only defect is corrected through a bounded task and revalidated before acceptance.

## Step 16 — Owner acceptance and unattended LIVE operation

After final project/owner acceptance, the MEME-LIVE production-readiness lane is complete.

The system must be able to consume the approved strategy/source continuously, admit only eligible bounded LIVE trades, execute/sign/send/reconcile real transactions, maintain actual position/accounting truth, execute exactly one selected exit policy per position, survive crash/network/RPC/reboot uncertainty, and continue operating unattended under the declared supported policy.

Ongoing real-capital size remains separately human-controlled. Passing T011 does not automatically authorize larger capital.

## Acceptance discipline

Bounded worker tasks receive LOCAL PASS only. Work packages/areas receive final acceptance only after SYSTEM TRANSITION PASS demonstrates compatibility with downstream lifecycle transitions.

No `not implemented`, `future provider`, `out of scope`, `paper-only`, `shadow-only`, `default deny` or equivalent limitation may be owner-accepted unless it is intentionally human/external or explicitly owned by a later lifecycle item.

This plan does not authorize production implementation, signing, send/broadcast or real capital. The next step is Step 1: corrected Production Closure Architecture / roadmap correction.