# FIX2 actual public DRY qualification

Engineering is `IMPLEMENTED_PENDING_PROJECT_REVIEW`. Actual public qualification
has not run: no concrete real clock policy has project approval. T010 remains
`NOT_STARTED`; T011, M59/M60/M61 and real capital remain `NOT_AUTHORIZED`.

## Explicit public interface

`scripts/live_runtime_dry_public_qualification_v0_1.py --request <absolute-json>
--output <new-absolute-json>` accepts only a
`MEME_LIVE_PUBLIC_DRY_QUALIFICATION_REQUEST_V1` request with `schema`, `profile`,
`facts`, `controls` and `review_reference`. The three inputs are absolute JSON
`path` plus exact file `sha256` references. `--verify` checks existing evidence
offline. See the CLI help for the exact mutually exclusive verification form.

The supported DRY profile binds the accepted deployment, extension and monitor;
current reviewed wallet baseline; exact retained canonical source identity and
anchors; five fresh isolated store paths; unchanged strategy/execution settings;
and explicit `PUBLIC_ENVIRONMENT_REVIEWED` substitutions with both synthetic
flags false. Its endpoint must equal the accepted public deployment endpoint.
There is no canonical-store initialization, source repair or fabricated candidate.

Facts contain `clock_profile`, `protective_timing`, `venue`, `token_program`,
`minimum_context_slot` and `source_lag_us`. The protective timing record must bind
the exact host, runtime/configuration and qualification digests. Startup, RSS and
disk observations are measured from the actual process. The original read-only
wallet adapter and canonical source adapter supply current observations.

The controls record is `MEME_LIVE_REVIEWED_DRY_CONTROLS_V1` with the profile content
digest, review reference and exactly original INSTALL_POLICY then ARM commands.
Only DRY grants matching the domain, policy, source and approved clock can bind.
No default grant, approved clock, balance, source anchor or protective timing is
manufactured by this interface. Clock/controls validation precedes store setup.

The finite qualifier initializes fresh isolated stores, then uses the original
producer, handoff, Authority, Runtime and Ledger. It requires two distinct original
admissions with zero-signature exact simulations. After the second durable
EXACT_SIMULATED stage it interrupts, closes, reopens through original owned
startup and retires the same attempt without another public execution call.
Candidate absence, stale facts, uncertain clock, health failure and exceeded
reviewed capacity fail closed. No synthetic replacement makes that run succeed.

`MEME_LIVE_PUBLIC_DRY_QUALIFICATION_V1` evidence binds source bytes, profile,
requests, original preparations/stages/full terminal receipts, two startup fences,
cold recovery, actual observations, resource minima/maxima and isolated store
hashes. Clock evidence includes the received SNTP packet and exact monotonic
projection for offline recomputation. Store hashes describe qualification time;
offline verification does not open mutable databases. Hashes provide integrity,
not authenticated project approval. Dossier/preflight still require project
review and explicit current authority records. M56/M57 are reopened.

The retained clock observations prove their individual packet/bound/sample
relationships. They do not establish an offline replay of the entire clock
checkpoint chain. Runtime admission and restart enforce original Authority
continuity; the original qualified Ledger hash preserves its retained history.

## Missing clock decision

See `MEME_LIVE_STEP12A_FIX2_CLOCK_DECISION_V1.json` for exact candidate identity,
fingerprint, observations, alternatives and proposed bounds. The host's W32Time
service was stopped; no settings were changed. Read-only probes found Cloudflare
SNTP technically viable and less uncertain than the configured Windows peer.

The recommendation uses a pinned Cloudflare address and explicit unauthenticated
honest-server/network trust. SNTP does not provide NTS authentication. Project
selection must approve the provider and all five ClockPolicy fields; the numeric
proposal is evidence for that decision, not production policy. Thirty-second
observations are not a hardware drift certification or a guarantee about future
network behavior. Provider failure invalidates calibration immediately; there is
no local wall-clock fallback. Restart uses the original Authority checkpoint and
an explicit new-epoch reconciliation without renewing authority.

## Public capacity contradiction

The accepted extension permits `TOMBSTONE_ROWS=1` and `HISTORY_BYTES=9086`. Two
distinct original producer admissions retain two immutable tombstones. The
deterministic engineering fixture measures two and the unamended public guard
correctly rejects it. This is a structural lower bound, not actual public
capacity qualification. The prior synthetic amendment (2 / 16384) is not public
approval, and FIX2 has not substituted it.

If project review selects a narrow public DRY adjustment, the profile now accepts
an explicit `MEME_LIVE_PUBLIC_DRY_GUARD_REVIEW_V1` reference. It must have authority
CHATGPT_PROJECT_REVIEW, decision APPROVED_FOR_PUBLIC_DRY, a real review reference,
exact accepted profile/extension references, the complete `guard_limits` mapping
and a hash-bound actual-public capacity measurement. Only HISTORY_BYTES and
TOMBSTONE_ROWS may change. Other accepted limits and all timing controls remain
identical. The measurement schema is
`MEME_LIVE_PUBLIC_DRY_CAPACITY_OBSERVATION_V1`, scope
ACTUAL_PUBLIC_READONLY_DRY_CAPACITY, with exact accepted_extension, observed
maxima for both dimensions and false signer/send/broadcast and canonical mutation
flags. No actual measurement or approval under these schemas was issued here.

The minimum tombstone requirement is two for qualification alone. No numerical
history or 12-hour capacity proposal can honestly be approved from that synthetic
fixture. Actual measurement and a project decision remain necessary; the existing
M52/M53 acceptance artifacts are immutable and were not requalified.

## Engineering validation and host handoff

The focused clock/qualifier tests use explicitly fake external observations on
disposable stores. The composed orchestration runs the complete qualifier with
original owners and fake external facts, labels its output TEST_ONLY, and proves
that public validation rejects it. This is engineering evidence, not a public run.
The frozen affected regression records exact command exits, outputs and source
bytes; it does not execute accepted Step10/Step11 campaigns.

After actual qualification and external project/authority decisions, the separate
host request binds the same profile, facts and original terminal receipts plus
an explicit current store/control handoff. See
`MEME_LIVE_T010_PUBLIC_HOST_RUNBOOK_V1.md` for launch, status, stop, reconciliation,
unchanged restart ownership and independent 12h/14h lifetime rules. Engineering
tests never launch T010.
